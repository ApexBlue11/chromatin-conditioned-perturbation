# TASK W20 — §88 execution code: fold-0 trainer seeding fix + the C8b post-pathway MoA probe (RESULTS §88)

## Read first
`model/results/RESULTS.md` section `## 88.` (binding: 88.1 design, 88.2 order of checks, 88.3 readings) and `## 86.`
(rows, positives, nulls — reused exactly). Code: `model/v9/train_v9_gpu.py` (`main()`), `model/v9/xpert_arm.py`
(`seed_devices`, `cuda_states_equal`, and how `--dp_seed_mode` records `cuda_rng_states_equal_by_epoch`),
`model/v9/probe_moa_v9.py` (the §86 probe — reuse its helpers, do NOT change its behaviour), `model/v9/model_v9.py`
(`cfg.post_pathway` → `aux['post_pathway_activations']`, shape `[B, P, d_pathway]`), `model/v9/test_probe_moa_v9.py`.

## Part A — `train_v9_gpu.py` (defaults must reproduce today's behaviour bitwise)
1. `--post_pathway` (store_true) → `cfg.post_pathway = True`. Print it in the startup line.
2. `--dp_seed_mode {lockstep,distinct}`, **default `lockstep`** (today's behaviour). `distinct` seeds device k with
   `seed + 1000·k` exactly as `xpert_arm.py` does. Reuse `seed_devices` / `cuda_states_equal`: import them from
   `xpert_arm` if that import is side-effect free and not circular; otherwise move the two functions verbatim into a new
   `model/v9/dp_seeding.py` and import them from there in BOTH files (xpert_arm's behaviour must not change).
3. `--tf32 {on,off}`, **default `on`** (today). `off` sets both `torch.backends.cuda.matmul.allow_tf32` and
   `torch.backends.cudnn.allow_tf32` to False.
4. After every epoch, when `n_gpu > 1`, record whether the device CUDA RNG states are equal; store the list, the device
   seeds, `dp_seed_mode`, `tf32` and `post_pathway` in the checkpoint (a `train_meta` dict) and print them per epoch.
   With `distinct` and `n_gpu > 1`, raise SystemExit if the states are equal after epoch 0 (the §85.5 guard).
5. The checkpoint's saved `cfg` must include `post_pathway` (check that `vars(cfg)` or equivalent is what is saved).

## Part B — new file `model/v9/probe_moa_88.py` (the §88 probe). Import helpers from `probe_moa_v9.py`
(`load_chembl_targets`, `parse_gmt_full_sets`, `project_diff`, `score`) — do not copy them.
1. **Inputs:** `--ckpt` (a C8b fold-0 checkpoint) or `--untrained --seed S --cfg_from CKPT` (build the model from that
   checkpoint's `cfg`, do NOT load its weights, `torch.manual_seed(S)`, fit the quantiser on training rows exactly as
   §86 does). Refuse (SystemExit) if the cfg has `post_pathway` False.
2. **Rows, compounds, positives, mean drug, `k̄`:** identical code path to `probe_moa_v9.py` (same selection, same
   positive sets, same mean-drug baseline). **Assert** the row sha1 equals `3e59a7ba7832775596f032dbab06c2c36a7723b4`
   (the §86 rows).
3. **Primary readout (§88.1 item 2), no gradients:** per row, `Δa[p] = ‖a_post(d)[p] − a_post(mean drug)[p]‖₂` over the
   `d_pathway` channels (`aux['post_pathway_activations']`, model in eval mode, `torch.no_grad()`); per compound the median
   over its ≤ 4 rows → `[D, P]`.
4. **Order of checks (§88.2), each written to the output JSON:**
   (1) degeneracy: `max Δa > 1e-12`, else `void: true` and stop scoring; (2) gate: median cross-compound Spearman of the
   Δa vectors over the same pair sampling as §86 (`rho_post`), `gate_pass = rho_post < 0.95`; (3) `score()` → S, Null 1,
   Null 2, diff, p; (4) **Null 1s:** label permutation **within quintiles of compound response strength**, 1,000
   permutations, returning its own mean, sd, `diff_s = S − mean` and `p_s = mean(perm ≤ S)`. Compound response strength =
   the median over the compound's scored rows of the row's mean over genes of `|y_delta|`. Quintile edges by
   `np.quantile` on the D compounds; a compound's positives may only be swapped with those of a compound in the same
   quintile. Add it as a new function `score_within_strata(scores, allpos, strata, rng_seed, n_perm)` in THIS file.
5. **Reference readouts (§88.1 item 6):** the output projection and data projection exactly as §86 computes them, each
   through `score()` and Null 1s.
6. **Strata (§88.1 item 7), each scored with `score()` AND Null 1s:** `unseen` / `seen` (compound absent/present in
   fold-0 training, as §86); target responsiveness: per compound, rank all 978 genes by the mean over its rows of
   `|y_delta|` (descending, percentile in [0, 1), 0 = most responsive), take the mean percentile of its landmark targets;
   compounds with no landmark target → group `no_landmark_target`; the rest split **at the median** of that value into
   `target_responsive` (≤ median) and `target_not_responsive` (> median). Report the n of every stratum.
7. Output `probe_moa_88_<stem>.json` (stem as §86: checkpoint basename or `untrained_s{S}`) with every number above,
   `row_sha1`, `k_med`, `n_compounds`, the cfg flags, and the Δa max. Kaggle `/kaggle/working` fallback as §86.

## Tests — `model/v9/test_probe_moa_88.py` (toy data / tiny models only; paste the output)
(a) `score_within_strata`: over 200 permutations every compound's positives come from a compound in the same stratum;
    with one stratum it matches an unrestricted permutation's distribution in mean (within 0.01 on a toy of 60);
(b) the Δa readout on a tiny LincsV9 with `post_pathway=True` equals a hand-computed per-row L2 norm of the activation
    difference and is exactly 0 when the drug inputs equal the mean-drug inputs;
(c) `--untrained` with a cfg lacking `post_pathway` raises SystemExit;
(d) the responsiveness strata: a compound with no landmark target lands in `no_landmark_target`; the other two groups
    differ in size by at most 1 on a toy with distinct values;
(e) `train_v9_gpu.py`: with defaults the argument parsing yields `dp_seed_mode='lockstep'`, `tf32='on'`,
    `post_pathway=False`; `distinct` on 2 fake devices gives seeds `[s, s+1000]` (CPU-only test of the seed list, no CUDA
    needed — factor the seed-list computation so it can be tested).
Also rerun `model/v9/test_probe_moa_v9.py`, `model/v9/test_candidates_v9.py`, `model/v9/test_xpert_arm_dev.py` and
`model/v9/test_c7_v9.py`; paste all outputs.

## Rules
Edit only `train_v9_gpu.py` (and `xpert_arm.py` only if you move the two helpers); create `probe_moa_88.py`,
`test_probe_moa_88.py`, and `dp_seeding.py` if needed. Interpreter `C:\Projects\LINCS\.venv-cuda\Scripts\python.exe`.
No installs, no training, no GPU runs, no git, no scratch files in the repository root. Report any place where RESULTS
§88 is ambiguous and what you chose — do not silently decide.
