# TASK W19 — C7 (chromatin-gated union-graph edges) and C7u (ungated), exactly as RESULTS §89.1

## Read first
`model/results/RESULTS.md` section `## 89.` (binding definition and acceptance tests); `model/v9/modules_v9.py`
(`PPIMessagePassing`, `StochasticDepth`, `NamedPathwayReadout`); `model/v9/model_v9.py` (`LincsV9.__init__/forward`,
where `self.ppi` is applied); `model/v9/xpert_arm.py` (`XPertData`: lines ~205-245 build `E` and `r` from `E_final.npy`
and `E_final_mask.npy`; `batch()`); `model/v9/test_candidates_v9.py` (how the §85.7 flags were tested — follow it).

## Deliverable
1. **Per-track observed mask.** `XPertData` currently collapses `E_final_mask` to `r` = "any track observed" per gene.
   Add `self.Em` = the per-gene, per-track boolean mask `[rows, G, 3]` (from `E_final_mask[j]`; all-False for cells with no
   chromatin row), and put it in the batch as `'E_mask'` (float). Leave `E` and `r` exactly as they are. Inspect what
   `E_final.npy` holds where the mask is False (zeros? NaN?) and report it; the gate must use `E * mask` so missing is 0.
2. **Module `ChromatinGatedUnionMP(edge_index, n_genes, d, stoch_depth, gated=True)`** in `modules_v9.py`:
   - graph from `network/outputs/v9/union_graph_v9.npz` `edge_index` (2 × 81,846): symmetrise, de-duplicate, drop
     self-loops; `Â = D^-1/2 A D^-1/2`; register as a (sparse or dense 978×978) buffer; record and print the final
     undirected edge count;
   - gate (only if `gated`): `a = sigmoid(MLP(6→8→1)([E*m, m]))` per gene, final bias initialised to logit(0.95) and the
     final weight initialised to zero (so a ≈ 0.95 at init); `a = 1` exactly where all three tracks are missing;
     `g_ij = sqrt(a_i a_j)`; for C7u (`gated=False`) `g ≡ 1`;
   - forward: `h + sd(W · (Â ∘ g) h)` with `W = nn.Linear(d, d)`, stochastic depth at `stoch_depth`.
3. **Flags** (`V9Config` fields + `xpert_arm.py` CLI): `--chromatin_edges` (C7) and `--union_edges` (C7u). Either one
   **replaces** the STRING step (`self.ppi` not built/applied; the new module sits exactly where `self.ppi` is applied).
   Mutually exclusive (assert). Output-name tags `_chromedges` / `_unionedges`; record in `candidate_flags`.
4. **Mismatched-chromatin scorer** for §89.2 item 3 (used later, only if C7 is accepted): a function in a new file
   `model/v9/chromatin_mismatch.py` that, given a C7 checkpoint, the dev rows and a donor mapping, re-scores each dev
   cell with each of the 5 other dev cells' chromatin: values swapped **only where both cells observe the track**; the
   evaluated cell keeps its own mask; where only the evaluated cell observes a track, use the donor's per-track mean over
   its observed genes. Write per-row own vs mismatched scores to an `.npz`. Test it on toy data only.

## Tests — `model/v9/test_c7_v9.py`
(a) both flags off → bitwise identical outputs and loss to the committed code (as test_candidates_v9 does);
(b) a cell with all tracks missing: C7's layer output equals C7u's exactly;
(c) toggling one track's indicator with its value at 0 changes that gene's `a_i`;
(d) changing gene k's chromatin changes only `g_kj` / `g_jk` (all other gates identical);
(e) `a ≈ 0.95` at init for observed genes;
(f) the union adjacency is symmetric, has no self-loops, and its row sums of `Â` are finite;
(g) the mismatch scorer on a 2-cell toy swaps only co-observed values and keeps the evaluated cell's mask.
Also rerun `model/v9/test_candidates_v9.py` and `model/v9/test_xpert_arm_dev.py`. Paste all outputs.

## Rules
Edit only `modules_v9.py`, `model_v9.py`, `config_v9.py`, `xpert_arm.py`; create `chromatin_mismatch.py` and
`test_c7_v9.py`. Interpreter `C:\Projects\LINCS\.venv-cuda\Scripts\python.exe`. No installs, no training, no GPU runs,
no scratch files in the repository root.
