# Superseded outputs

Kept, not read, so that forking paths stay visible [review 016 C2].

- `a1_fullgraph_pretest_nanbug*` -- the first full run of `model/v9/a1_fullgraph_pretest.py`. Its primary contrast was
  NaN (random draws on isolated nodes). Superseded by the run after the fix at commit 0506102; RESULTS 83.5.
